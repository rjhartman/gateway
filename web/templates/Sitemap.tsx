import type { FC, ReactNode } from 'react'
import Layout from '@common/Layout'

const Sitemap: FC<PageTemplateProps> = ({ ...rest }) => {
  return <Layout {...rest}></Layout>
}

export default Sitemap
